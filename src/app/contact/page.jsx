import Contact from "@/components/Contact";

export const metadata = {
    title: "Contact",
    description: "Contact | Tashdik-WEB",
  };

const page = () => {

    return (
        <div>
            <Contact></Contact>
        </div>
    );
};

export default page;